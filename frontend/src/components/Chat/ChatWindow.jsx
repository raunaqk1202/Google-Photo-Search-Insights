/**
 * ChatWindow — placeholder component for the conversational interface.
 * Full implementation in Phase 5.
 */

export default function ChatWindow() {
  return (
    <div style={{ padding: "2rem", textAlign: "center", color: "#94a3b8" }}>
      <h2>💬 Chat Interface</h2>
      <p>Conversational RAG interface will be implemented in Phase 5.</p>
    </div>
  );
}
